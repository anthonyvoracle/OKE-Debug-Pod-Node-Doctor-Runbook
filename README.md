# OKE-Debug-Pod-Node-Doctor-Runbook
This runbook shows how to setup a debug pod on your OKE node and use it to grab diagnostics on an instance where you may not have ability to SSH directly into the host. 
